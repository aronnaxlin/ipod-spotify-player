import { create } from 'zustand';

// Mock dependencies
const isMobileSafari = false;

export const useIsPhone = () => false;

export const parseLyricTimestamps = (text: string) => [];
export const findCurrentLineIndex = (lines: any[], time: number) => 0;

export const toRomaji = (text: string) => text;
