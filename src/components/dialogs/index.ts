import React from 'react';

export const HelpDialog = (props: any) => null;
export const AboutDialog = (props: any) => null;
export const ConfirmDialog = (props: any) => null;
export const ShareItemDialog = (props: any) => null;
export const LyricsSearchDialog = (props: any) => null;
export const SongSearchDialog = (props: any) => null;
export interface SongSearchResult {
    videoId: string;
}
